# Minimal dependencies for the data smoke test (this case)
numpy>=1.22
matplotlib>=3.5

# Optional (not required for the smoke test / --plot-only)
# ovito   # only if recomputing this case from dump.xyz
